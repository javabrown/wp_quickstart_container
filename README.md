# wp_quickstart_container
Quick Start - docker container configuration for WordPress, MySql and PHPAdmin to start application from scratch 

### Run following command to begin:
`docker-compose up | down`
